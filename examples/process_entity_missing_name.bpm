process "Process Entity Missing Name" {
    id: "process-entity-missing-name"
    version: "1.0"

    start "Start Process" {
        id: "start-event"
    }

    processEntity "Load Customer Data" {
        entityModel: "/api/specs/customer-api.yaml"
        // Missing entityName
    }

    end "End Process" {
        id: "end-event"
    }

    flow {
        "start-event" -> "load-customer-data"
        "load-customer-data" -> "end-event"
    }
}
